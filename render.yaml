services:
  - type: web
    name: ohara
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /api/saude
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.5
